name: multitask-misinformation-detection

channels:
  - conda-forge
  - pytorch

dependencies:
  # ========================================================
  # Python
  # ========================================================
  - python=3.10

  # ========================================================
  # Core Scientific Stack
  # ========================================================
  - numpy>=1.26,<3.0
  - pandas>=2.2,<3.0
  - scikit-learn>=1.4,<2.0
  - matplotlib>=3.8,<4.0
  - tqdm>=4.66,<5.0

  # ========================================================
  # Jupyter
  # ========================================================
  - jupyter
  - notebook>=7.0
  - ipywidgets>=8.1,<9.0

  # ========================================================
  # Deep Learning
  # ========================================================
  - pytorch>=2.2

  # ========================================================
  # Pip Packages
  # ========================================================
  - pip

  - pip:
      - transformers>=4.40,<5.0
      - datasets>=2.18,<3.0
      - tokenizers>=0.15,<1.0
      - accelerate>=0.29,<1.0